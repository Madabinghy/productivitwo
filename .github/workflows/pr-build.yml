name: PR Build (web)

# Filet de validation des PR : compile le Flutter web (SANS déployer) dès que le
# code Dart change. Échoue sur une vraie erreur de compilation, pas sur un warning.
# Sert notamment à valider les refactors mécaniques (split de god-files) avant merge.
#
# Coût minutes maîtrisé :
#  - ne tourne QUE si lib/ ou le pubspec changent (paths)
#  - concurrency annule les runs obsolètes d'une même PR (push successifs)
on:
  pull_request:
    paths:
      - "lib/**"
      - "test/**"
      - "pubspec.yaml"
      - "pubspec.lock"
      - ".github/workflows/pr-build.yml"

concurrency:
  group: pr-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.35.6"
          channel: stable
          cache: true

      - name: flutter pub get
        run: flutter pub get

      # Échoue sur les ERREURS d'analyse seulement (les warnings hérités ne
      # bloquent pas — durcir en retirant le filtre quand ils seront purgés).
      - name: flutter analyze (erreurs seulement)
        run: |
          flutter analyze --no-pub 2>&1 | tee analyze.log
          if grep -q "error •" analyze.log; then
            echo "::error::flutter analyze a trouvé des erreurs"; exit 1
          fi

      - name: flutter test
        run: flutter test

      - name: Build web (compile only, no deploy)
        run: flutter build web --release --no-tree-shake-icons
